'use strict';

module.exports = {
  version: 1,
  description: 'Sound Board — no database tables needed (UI-only addon)',

  up(db) {
    // No tables required — Sound Board is purely a UI component
  },

  down(db) {
    // Nothing to drop
  }
};
